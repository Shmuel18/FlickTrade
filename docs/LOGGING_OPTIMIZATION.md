# תיקוני אופטימיזציה - הפחתת עומס לוגים

## בעיה שנפתרה

הבוט היה מייצר כמות אדירה של לוגים (DEBUG level) שגרמה לעומס על המחשב ובעיות ביצועים.

## השינויים שבוצעו

### 1. תצורת Logging חדשה (`logging_config.py`)

- ✅ **Log Rotation**: מקסימום 5MB לקובץ, 3 גיבויים (20MB סה"כ)
- ✅ **רמת INFO בלבד**: ללא DEBUG logs שמנפחים את המערכת
- ✅ **פורמט עדין**: הודעות קצרות וממוקדות
- ✅ **הפרדה בין Console ו-File**: Console מראה רק חשוב, File שומר הכל

### 2. הסרת DEBUG Logs מיותרים

הוסרו מאות שורות DEBUG מ:

- `main.py` - הסרת לוגי cooldown, debug tokens
- `logic.py` - הסרת לוגי בדיקת מחירים
- `scanner.py` - הסרת לוגי ניתוח שווקים
- `persistence.py` - הסרת לוג לכל טרנזקציה

### 3. אופטימיזציות נוספות

- **מופחת logging של price updates** - לא לוג על כל עדכון מחיר
- **מופחת logging של opportunities** - רק כשממש נמצא משהו
- **שקט יותר בזמן ריצה** - רק הודעות קריטיות

## איך להשתמש

```bash
python run_bot.py
```

הבוט יתחיל עם המסר:

```
🤖 POLYMARKET ARBITRAGE BOT - OPTIMIZED MODE
✅ Reduced logging to prevent system overload
✅ Log rotation enabled (max 20MB total)
✅ Only INFO and higher messages shown
```

## מבנה הקבצים

```
logs/
  ├── bot.log          # קובץ לוג עיקרי (מקסימום 5MB)
  ├── bot.log.1        # גיבוי 1
  ├── bot.log.2        # גיבוי 2
  └── bot.log.3        # גיבוי 3
```

כאשר `bot.log` מגיע ל-5MB, הוא מועבר ל-`bot.log.1` והישן נמחק.

## יתרונות

1. **ביצועים טובים יותר** - פחות כתיבה לדיסק
2. **נפח קבצים מוגבל** - לא יותר עשרות GB של לוגים
3. **קריאות טובה יותר** - רק מה שחשוב
4. **יציבות** - לא יקריס את המחשב

## רמות Logging

| רמה     | מתי מוצג                                    |
| ------- | ------------------------------------------- |
| ERROR   | תמיד - שגיאות קריטיות                       |
| WARNING | תמיד - אזהרות חשובות                        |
| INFO    | תמיד - אירועים חשובים (trades, connections) |
| DEBUG   | **לעולם לא** - הושבת לחלוטין                |

## דוגמאות להודעות שיוצגו

✅ **יוצגו:**

```
[OPPORTUNITY] BTC > 100k | Profit: $1.23 (2.5%)
[SUCCESS] Trade executed! Estimated profit: $1.23
[STATS] Updates: 1500 | Opportunities: 5 | Trades: 2
```

❌ **לא יוצגו:**

```
[DEBUG] Event: Bitcoin price prediction...
[DEBUG] token_ids type: list, value: ['0x123...']
[SKIP] pair - Cooldown active (45s ago)
```

---

**תאריך עדכון:** 2026-01-07
**גרסה:** 2.0 - Optimized
